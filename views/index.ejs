<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>Skill Up — Learn Web Dev, Fast</title>
  <link rel="stylesheet" href="/css/styles.css">
  
</head>

<body>
  <%- include('partials/header') %>

  <main class="hero">
    <h2>Level-up your web skills in weeks, not months.</h2>
    <p>Join interactive, project-based courses led by industry pros. Build a real-world portfolio while learning HTML, CSS, JavaScript & Node.</p>

    <a href="/courses" class="cta-btn">Browse Courses</a>

    <!-- live clock placeholder -->
    <div class="clock" id="liveClock"></div>
  </main>

  <script>
/* Live date & time (Europe/London) */
function updateClock() {
  const now = new Date();
  const options = {
    timeZone: 'Europe/London',
    weekday: 'short',
    year: 'numeric',
    month: 'short',
    day: '2-digit',
    hour: '2-digit',
    minute: '2-digit',
    second: '2-digit'
  };
  document.getElementById('liveClock').textContent =
    now.toLocaleString('en-GB', options).replace(',', ' —');
}
updateClock();                 // initial fill
setInterval(updateClock, 1000); // update every second
</script>

</body>
</html>
